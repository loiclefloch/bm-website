var React = require('react');

var NoTag = React.createClass({

  render: function () {

    return (
      <div>
        <h3>There is no tag</h3>
      </div>
    );
  }

});

module.exports = NoTag;